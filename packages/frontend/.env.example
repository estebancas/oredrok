# Email Worker URL
# In production, set this to your deployed worker URL (e.g., https://oredrok-email-worker.your-subdomain.workers.dev)
# In development, the default is http://localhost:8788/contact
PUBLIC_WORKER_URL=http://localhost:8788/contact
